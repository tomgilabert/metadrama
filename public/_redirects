# Redirecciones para Cloudflare Pages

# Si migras el dominio pero recibes tráfico residual con el prefijo /metadrama/
# del sitio viejo de UB, redirige a la URL limpia.
/metadrama/*  /:splat  301

# /admin -> /admin/ (trailing slash para Decap CMS)
/admin  /admin/  301
